# 単体テスト仕様書 — 図書館蔵書チェッカー

バージョン: 1.0.0
対象コード: `extension/` 配下の全 JavaScript ファイル

---

## 1. 環境構築

### 1.1 前提条件

| ツール | バージョン | 備考 |
|-------|-----------|------|
| Node.js | 20.x LTS 以上 | ARM64環境では arm64 ビルドを使用 |
| npm | 10.x 以上 | Node.js に同梱 |

### 1.2 Node.js のインストール

#### macOS / Linux (x86_64)

```bash
# Node.js 公式サイト https://nodejs.org/ からインストーラを取得するか、
# nvm (Node Version Manager) を使用する
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
nvm install 20
nvm use 20
```

#### Linux (ARM64 / aarch64)

本プロジェクトの開発環境は `aarch64` アーキテクチャのため、ARM64用バイナリを使用する。

```bash
# アーキテクチャ確認
uname -m   # → aarch64 であることを確認

# Node.js 20 LTS (ARM64) をダウンロードして /usr/local に展開
curl -fsSL https://nodejs.org/dist/v20.18.0/node-v20.18.0-linux-arm64.tar.gz \
  -o /tmp/node.tar.gz
tar -xzf /tmp/node.tar.gz -C /usr/local --strip-components=1

# バージョン確認
node --version   # → v20.18.0
npm --version    # → 10.8.2
```

> x86_64 環境のバイナリ（`node-vXX.X.X-linux-x64.tar.gz`）を ARM64 環境で実行すると Rosetta エラーが発生するため、アーキテクチャに合ったビルドを使うこと。

### 1.3 依存パッケージのインストール

リポジトリのルート（`package.json` が存在するディレクトリ）で実行する。

```bash
cd /workspaces/web-library-checker
npm install
```

インストールされるパッケージ:

| パッケージ | バージョン | 用途 |
|-----------|-----------|------|
| `jest` | ^29.7.0 | テストランナー・アサーション・モック |
| `jest-environment-jsdom` | ^29.7.0 | ブラウザDOM環境のエミュレーション |

### 1.4 ディレクトリ構成（テスト関連ファイル）

```
web-library-checker/
├── package.json                        # npm 設定・依存定義
├── jest.config.js                      # Jest 設定
└── tests/
  ├── setup.js                        # Chrome API グローバルモック
  ├── helpers/
  │   └── load-content-script.js      # content_script.js の IIFE アンラップローダー
  ├── isbn.test.js
  ├── sanitize.test.js
  ├── cache.test.js
  ├── widget.test.js
  ├── calil.test.js
  ├── service_worker.test.js
  └── popup.test.js
```

### 1.5 テストの実行

```bash
# PATH に Node.js を追加（インストール先によって調整）
export PATH="/usr/local/bin:$PATH"

# 全テストを実行
npm test
# または
npx jest

# ウォッチモード（ファイル変更を検知して自動再実行）
npm run test:watch

# カバレッジレポート付きで実行
npm run test:coverage
```

### 1.6 Jest 設定（`jest.config.js`）

```js
module.exports = {
  testEnvironment: 'jsdom',           // ブラウザ相当の DOM 環境を使用
  setupFiles: ['./extension/__tests__/setup.js'],  // Chrome API モックを事前ロード
  testMatch: ['**/extension/__tests__/**/*.test.js'],
  collectCoverageFrom: [
    'extension/api/calil.js',
    'extension/background/service_worker.js',
    'extension/content/content_script.js',
    'extension/popup/popup.js',
  ],
  coverageDirectory: 'coverage',
  coverageReporters: ['text', 'html'],
  testTimeout: 10000,
};
```

### 1.7 カバレッジの制限事項

本プロジェクトのソースコードは Chrome 拡張機能用に設計されており、次の理由でカバレッジが正しく計測されない。

| ファイル | 読み込み方式 | カバレッジ計測 |
|---------|------------|-------------|
| `content_script.js` / `popup.js` | `eval()` でIIFEをアンラップ | **計測不可**（Jestのインストルメンテーション対象外） |
| `calil.js` / `service_worker.js` | `vm.runInContext()` | **計測不可**（同上） |

`npm run test:coverage` を実行すると全ファイルが 0% と表示されるが、テストは正常に実行・検証されている。カバレッジを計測するには、ソースコードを CommonJS 形式（`module.exports`）または ES Modules 形式（`export`）にリファクタリングする必要がある。

### 1.8 実行結果の確認

正常時の出力例:

```
PASS extension/__tests__/isbn.test.js
PASS extension/__tests__/sanitize.test.js
PASS extension/__tests__/cache.test.js
PASS extension/__tests__/widget.test.js
PASS extension/__tests__/calil.test.js
PASS extension/__tests__/service_worker.test.js
PASS extension/__tests__/popup.test.js

Test Suites: 7 passed, 7 total
Tests:       144 passed, 144 total
Time:        ~1s
```

---

## 2. テスト方針

### テスト対象の分類

| 分類 | 対象関数の特徴 | テスト難易度 |
|------|-------------|------------|
| **純粋関数** | 入力→出力のみ、副作用なし | 低（最優先） |
| **DOM操作関数** | `document` への読み書きを伴う | 中（jsdom使用） |
| **非同期・IO関数** | `fetch`・`chrome.*` APIを呼ぶ | 高（モック必須） |

純粋関数から優先してテストを整備し、DOM操作・非同期関数はモック戦略を定めた上でカバーする。

### 推奨テストフレームワーク

| ツール | 用途 |
|-------|------|
| [Jest](https://jestjs.io/) | テストランナー・アサーション・モック |
| [jsdom](https://github.com/jsdom/jsdom) | ブラウザDOM環境のエミュレーション（Jest に同梱） |

### Chrome拡張機能APIのモック方針

Chrome拡張機能API（`chrome.storage`、`chrome.runtime`、`chrome.tabs`）はテスト環境に存在しないため、次の方針でモックする。

```js
// jest.setup.js（各テストファイルの前に実行）
global.chrome = {
  storage: {
    sync: {
      get: jest.fn(),
      set: jest.fn(),
    },
  },
  runtime: {
    sendMessage: jest.fn(),
    onMessage: { addListener: jest.fn() },
    lastError: null,
    openOptionsPage: jest.fn(),
  },
  tabs: {
    query: jest.fn(),
    sendMessage: jest.fn(),
  },
};
```

### ファイル構成（テストコード）

```
tests/
├── isbn.test.js               # ISBN変換・抽出ユーティリティ
├── sanitize.test.js           # XSSサニタイズ・URLバリデーション
├── cache.test.js              # セッションキャッシュ
├── widget.test.js             # ウィジェット表示ロジック
├── calil.test.js              # カーリルAPIモジュール
├── service_worker.test.js     # サービスワーカーのメッセージハンドラ
└── popup.test.js              # ポップアップの集約表示ロジック
```

---

## 2. ISBN変換・抽出ユーティリティ（`content_script.js` インライン関数）

対象関数: `calcIsbn13CheckDigit` / `isbn10to13` / `normalizeIsbn` / `toIsbn13` / `extractAsinFromUrl`

### UT-ISBN-01: `calcIsbn13CheckDigit` — ISBN-13チェックデジット計算

```
関数シグネチャ: calcIsbn13CheckDigit(digits12: string) → number
```

| # | 入力 `digits12` | 期待値 | 説明 |
|---|----------------|--------|------|
| 01 | `"978487311738"` | `6` | 『オライリー本』ISBN-13の最終桁 |
| 02 | `"978404892481"` | 0〜9の整数 | 結果が0以上9以下の整数であること（汎用検証） |
| 03 | `"978020163361"` | `0` | チェックデジットが0になるケース |
| 04 | `"978030640615"` | `7` | 別の実例 |
| 05 | `"978487311738"` | 0〜9の整数 | 同一入力で0以上9以下の範囲確認 |

**計算ロジック検証（01の例）:**
```
位置 0(奇): 9×1= 9
位置 1(偶): 7×3=21
位置 2(奇): 8×1= 8
位置 3(偶): 4×3=12
位置 4(奇): 8×1= 8
位置 5(偶): 7×3=21
位置 6(奇): 3×1= 3
位置 7(偶): 1×3= 3
位置 8(奇): 1×1= 1
位置 9(偶): 7×3=21
位置10(奇): 3×1= 3
位置11(偶): 8×3=24
合計 = 134, 134 % 10 = 4, (10 - 4) % 10 = 6 ✓
```

---

### UT-ISBN-02: `normalizeIsbn` — ハイフン・スペース除去

```
関数シグネチャ: normalizeIsbn(isbn: string) → string
```

| # | 入力 | 期待値 | 説明 |
|---|------|--------|------|
| 01 | `"978-4-87311-738-6"` | `"9784873117386"` | ハイフン除去 |
| 02 | `"978 4 87311 738 6"` | `"9784873117386"` | スペース除去 |
| 03 | `"978 4-87311-738-6"` | `"9784873117386"` | ハイフンとスペース混在 |
| 04 | `"9784873117386"` | `"9784873117386"` | プレーンなISBNはそのまま |
| 05 | `""` | `""` | 空文字はそのまま |

---

### UT-ISBN-03: `isbn10to13` — ISBN-10からISBN-13への変換

```
関数シグネチャ: isbn10to13(isbn10: string) → string | null
```

| # | 入力 | 期待値 | 説明 |
|---|------|--------|------|
| 01 | `"4873117380"` | `"9784873117386"` | 標準的なISBN-10 |
| 02 | `"080442957X"` | `"9780804429573"` | チェックデジットがXのISBN-10（Xを10として扱う） |
| 03 | `"4-87311-738-0"` | `"9784873117386"` | ハイフン付きISBN-10（内部で正規化して変換） |
| 04 | `"12345"` | `null` | 桁数不足（5桁）→ null |
| 05 | `""` | `null` | 空文字 → null |
| 06 | `"9784873117386"` | `null` | 13桁入力は対象外 → null |

---

### UT-ISBN-04: `toIsbn13` — 任意ISBN文字列のISBN-13正規化

```
関数シグネチャ: toIsbn13(isbn: string) → string | null
```

| # | 入力 | 期待値 | 説明 |
|---|------|--------|------|
| 01 | `"9784873117386"` | `"9784873117386"` | 正しいISBN-13はそのまま |
| 02 | `"978-4-87311-738-6"` | `"9784873117386"` | ハイフン付きISBN-13 |
| 03 | `"4873117380"` | `"9784873117386"` | ISBN-10を変換 |
| 04 | `"487311738X"` | `"9784873117386"` | X末尾のISBN-10 |
| 05 | `""` | `null` | 空文字 → null |
| 06 | `"12345"` | `null` | 桁数不足（短い文字列）→ null |
| 07 | `"notanisbn"` | `null` | 数字以外の文字列 → null |
| 08 | `"12345678901"` | `null` | 11桁（10桁でも13桁でもない）→ null |
| 09 | `"12345678901234"` | `null` | 14桁 → null |
| 10 | `"9791032309285"` | `"9791032309285"` | 979プレフィックスのISBN-13はそのまま |

---

### UT-ISBN-05: `extractAsinFromUrl` — AmazonページURLからのASIN抽出

```
関数シグネチャ: extractAsinFromUrl(url: string) → string | null
```

| # | 入力 `url` | 期待値 | 説明 |
|---|-----------|--------|------|
| 01 | `"https://www.amazon.co.jp/dp/4873117380"` | `"4873117380"` | 標準的なURL |
| 02 | `"https://www.amazon.co.jp/本のタイトル/dp/4873117380/ref=..."` | `"4873117380"` | パスにタイトルを含むURL |
| 03 | `"https://www.amazon.co.jp/dp/B09JQMJHXY"` | `"B09JQMJHXY"` | 英数字混在ASIN |
| 04 | `"https://www.amazon.co.jp/s?k=python"` | `null` | 検索ページ（`/dp/` なし） |
| 05 | `"https://example.com/product/12345"` | `null` | `/dp/` パスがないURL → null |
| 06 | `""` | `null` | 空文字 → null |

---

## 3. サニタイズ関数（`content_script.js` / `popup.js` 共通ロジック）

対象関数: `sanitizeText` / `sanitizeUrl`

### UT-SAN-01: `sanitizeText` — XSSテキストサニタイズ

```
関数シグネチャ: sanitizeText(text: string) → string（HTMLエスケープ済み）
```

jsdom環境で `document.createElement('div')` が使えること前提。

| # | 入力 | 期待値 | 説明 |
|---|------|--------|------|
| 01 | `"東京都立図書館"` | `"東京都立図書館"` | 通常文字列はそのまま |
| 02 | `"<script>alert(1)</script>"` | `"&lt;script&gt;alert(1)&lt;/script&gt;"` | scriptタグエスケープ |
| 03 | `'<img src=x onerror="alert(1)">'` | エスケープされた文字列 | imgタグエスケープ |
| 04 | `"A & B"` | `"A &amp; B"` | `&` エスケープ |
| 05 | `'"quoted"'` | `"&quot;quoted&quot;"` または `'"quoted"'` | 実装依存（createTextNodeなら変換なし） |
| 06 | `""` | `""` | 空文字 |
| 07 | `123` （数値） | `"123"` | `String()` で変換されること |

> `createTextNode` → `innerHTML` 経由のため、`<`, `>`, `&` はエスケープされ、`"` はエスケープされない。テスト時は実装に合わせた期待値を設定する。

---

### UT-SAN-02: `sanitizeUrl` — URLプロトコルバリデーション

```
関数シグネチャ: sanitizeUrl(url: string) → string
```

| # | 入力 | 期待値 | 説明 |
|---|------|--------|------|
| 01 | `"https://calil.jp/reserve/12345"` | `"https://calil.jp/reserve/12345"` | 正常なhttps URL |
| 02 | `"http://calil.jp/reserve/12345"` | `"http://calil.jp/reserve/12345"` | httpも許可 |
| 03 | `"javascript:alert(1)"` | `"#"` | javascriptスキーム → 無効化 |
| 04 | `"data:text/html,<script>alert(1)</script>"` | `"#"` | dataスキーム → 無効化 |
| 05 | `"ftp://example.com/file"` | `"#"` | ftp → 無効化 |
| 06 | `"not-a-url"` | `"#"` | 不正なURL → 無効化 |
| 07 | `""` | `"#"` | 空文字 → 無効化 |
| 08 | `"https://example.com/path?q=a&b=c"` | 正規化されたhttps URL | クエリ文字列付きURL |

---

## 4. ウィジェット表示ロジック（`content_script.js`）

### UT-WID-01: `getBranchIconClass` — 貸出状態からCSSクラス名への変換

```
関数シグネチャ: getBranchIconClass(status: string) → string
```

| # | 入力 `status` | 期待値 | 説明 |
|---|--------------|--------|------|
| 01 | `"貸出可"` | `"available"` | 緑アイコン |
| 02 | `"貸出中"` | `"on-loan"` | 黄アイコン |
| 03 | `"休館中"` | `"none"` | グレーアイコン |
| 04 | `"館内のみ"` | `"other"` | 青アイコン |
| 05 | `"予約中"` | `"other"` | 青アイコン |
| 06 | `"準備中"` | `"other"` | 青アイコン |
| 07 | `"蔵書なし"` | `"other"` | 青アイコン（none ではない） |
| 08 | `"未知のステータス"` | `"other"` | 未定義ステータス → other |
| 09 | `""` | `"other"` | 空文字 → other |

---

### UT-WID-02: `getCacheKey` — セッションストレージキーの生成

```
関数シグネチャ: getCacheKey(isbn: string, libraries: Array<{systemid: string}>) → string
```

| # | 入力 `isbn` | 入力 `libraries` | 期待値 |
|---|------------|-----------------|--------|
| 01 | `"9784873117386"` | `[{systemid:"Tokyo_Pref"}]` | `"calil_9784873117386_Tokyo_Pref"` |
| 02 | `"9784873117386"` | `[{systemid:"Osaka_Pref"},{systemid:"Tokyo_Pref"}]` | `"calil_9784873117386_Osaka_Pref,Tokyo_Pref"` |
| 03 | `"9784873117386"` | `[{systemid:"Tokyo_Pref"},{systemid:"Osaka_Pref"}]` | `"calil_9784873117386_Osaka_Pref,Tokyo_Pref"` | **順不同でもソートされる** |
| 04 | `"9784873117386"` | `[]` | `"calil_9784873117386_"` | 図書館なし |
| 05 | `"9784873117386"` | `[{systemid:"Tokyo_Pref"},{systemid:"Chiba_Pref"},{systemid:"Osaka_Pref"}]` | `"calil_9784873117386_Chiba_Pref,Osaka_Pref,Tokyo_Pref"` | 3件のアルファベット順ソート |

---

### UT-WID-03: `loadFromCache` / `saveToCache` — セッションストレージキャッシュ

jsdom 環境では `sessionStorage` が利用可能。

#### saveToCache

| # | 操作 | 期待結果 |
|---|------|---------|
| 01 | `saveToCache("key1", [{library:{name:"東京"}, result:{status:"OK"}, error:null}])` | `sessionStorage.getItem("key1")` が JSON 文字列として取得できる |
| 02 | `saveToCache("key1", null)` | `sessionStorage.getItem("key1")` が `"null"` になる |

#### loadFromCache

| # | 操作 | 期待結果 |
|---|------|---------|
| 03 | 事前に `sessionStorage.setItem("key1", JSON.stringify([{a:1}]))` → `loadFromCache("key1")` | `[{a:1}]` が返る |
| 04 | `sessionStorage` に存在しないキーで `loadFromCache("nokey")` | `null` が返る |
| 05 | `sessionStorage.setItem("bad", "not-json")` → `loadFromCache("bad")` | `null` が返る（JSON.parseエラーを握り潰す） |
| 06 | `saveToCache(key, data)` → 同じキーで `loadFromCache` を複数回呼ぶ | 毎回同じデータが返る（永続性の確認） |
| 07 | `saveToCache(key, {version:1})` → `saveToCache(key, {version:2})` → `loadFromCache(key)` | `{version:2}` が返る（上書き確認） |
| 08 | `saveToCache("key_a", {id:"a"})` と `saveToCache("key_b", {id:"b"})` → 各キーで `loadFromCache` | キーごとに独立した値が返る |

---

### UT-WID-04: `setWidgetResults` — ウィジェット結果DOM生成（jsdom使用）

jsdom で `#calil-library-checker` を含む最小DOMを構築した上でテストする。

#### 正常系: 複数館がある場合

**入力:**
```js
results = [{
  library: { systemid: "Tokyo_Pref", name: "東京都立図書館" },
  result: {
    status: "OK",
    reserveurl: "https://calil.jp/reserve/123",
    libkey: { "中央図書館": "貸出可", "多摩図書館": "貸出中" }
  },
  error: null
}]
```

| # | 確認内容 | 期待値 |
|---|---------|--------|
| 01 | `.calil-system-block` の個数 | `1` |
| 02 | `.calil-system-name` のテキスト | `"東京都立図書館"` |
| 03 | `.calil-reserve-link` の `href` | `"https://calil.jp/reserve/123"` |
| 04 | `.calil-branch-row` の個数 | `2` |
| 05 | 1行目 `.calil-branch-name` のテキスト | `"中央図書館"` |
| 06 | 1行目 `.calil-status-icon` のクラス | `available` を含む |
| 07 | 1行目 `.calil-branch-status` のクラス | `available` を含む |
| 08 | 2行目 `.calil-branch-name` のテキスト | `"多摩図書館"` |
| 09 | 2行目 `.calil-status-icon` のクラス | `on-loan` を含む |

#### 蔵書なし（libkey が空）

**入力:**
```js
results = [{
  library: { systemid: "Kyoto_Pref", name: "京都府立図書館" },
  result: { status: "OK", reserveurl: "", libkey: {} },
  error: null
}]
```

| # | 確認内容 | 期待値 |
|---|---------|--------|
| 10 | `.calil-branch-row` の個数 | `1` |
| 11 | `.calil-status-icon` のクラス | `none` を含む |
| 12 | `.calil-branch-status` のテキスト | `"この図書館には蔵書がありません"` |
| 13 | `.calil-reserve-link` の存在 | 存在しない |

#### status が "Error" の場合

**入力:**
```js
results = [{
  library: { systemid: "Chiba_Pref", name: "千葉県立図書館" },
  result: { status: "Error", libkey: null, reserveurl: "" },
  error: null
}]
```

| # | 確認内容 | 期待値 |
|---|---------|--------|
| 14 | `.calil-body` のテキスト | `"この図書館には蔵書がありません"` を含む |
| 15 | `.calil-status-icon` のクラス | `none` を含む |

#### APIエラー時（error フィールドあり）

**入力:**
```js
results = [{
  library: { systemid: "Saitama_Pref", name: "埼玉県立図書館" },
  result: null,
  error: "network error"
}]
```

| # | 確認内容 | 期待値 |
|---|---------|--------|
| 16 | `.calil-body` のテキスト | `"確認に失敗しました"` を含む |
| 17 | `.calil-status-icon` のクラス | `error` を含む |

#### results が空配列・null

| # | 確認内容 | 期待値 |
|---|---------|--------|
| 18 | results が `[]` のとき `.calil-body` のテキスト | `"蔵書情報を取得できませんでした"` を含む |
| 19 | results が `null` のとき `.calil-body` のテキスト | `"蔵書情報を取得できませんでした"` を含む |

#### 予約URLの有無

| # | 確認内容 | 期待値 |
|---|---------|--------|
| 20 | `reserveurl` に有効なURLが設定されている場合 | `.calil-reserve-link` の `href` がそのURLと一致する |
| 21 | `reserveurl` が空文字 `""` の場合 | `.calil-reserve-link` が存在しない |
| 22 | `reserveurl` が `null` の場合 | `.calil-reserve-link` が存在しない |

#### 複数図書館

| # | 確認内容 | 期待値 |
|---|---------|--------|
| 23 | 2図書館分の results を渡した場合 | `.calil-system-block` が2個生成される |

#### XSS対策確認

**入力:** `library.name = '<script>alert(1)</script>'`

| # | 確認内容 | 期待値 |
|---|---------|--------|
| 24 | DOM内に `<script>` 要素が存在しないこと | `widget.querySelectorAll('script')` が空 |
| 25 | `.calil-system-name` の innerHTML | `&lt;script&gt;` としてエスケープされている |

---

## 5. カーリルAPIモジュール（`api/calil.js`）

モック方針: グローバルの `fetch` を `jest.fn()` で差し替える。

### UT-API-01: `parseResponse` — JSON/JSONP両対応パース

```
関数シグネチャ: parseResponse(response: Response) → Promise<any>
```

`response` は `text()` メソッドを持つオブジェクトとしてモックする。

| # | `response.text()` の戻り値 | 期待戻り値 | 説明 |
|---|--------------------------|-----------|------|
| 01 | `'{"continue":0,"books":{}}'` | `{continue:0, books:{}}` | 正規JSONパース |
| 02 | `'callback({"continue":0,"books":{}})'` | `{continue:0, books:{}}` | JSONP形式（`callback`） |
| 03 | `'cb({"key":"val"})'` | `{key:"val"}` | 別名コールバック |
| 04 | `'cb({"key":"val"});'` | `{key:"val"}` | セミコロン付きJSONP |
| 05 | `'cb({"key":"val"})  '` | `{key:"val"}` | 末尾スペース付きJSONP |
| 06 | `'not valid'` | `throw Error` | 不正なフォーマット → エラー |
| 07 | `'[{"systemid":"Tokyo"}]'` | `[{systemid:"Tokyo"}]` | JSON配列（/libraryレスポンス） |
| 08 | `'callback([{"systemid":"Tokyo"}])'` | `[{systemid:"Tokyo"}]` | JSONP配列 |

---

### UT-API-02: `fetchWithRetry` — exponential backoff付きリトライ

`fetch` と `setTimeout`（sleep相当）をモックする（`setTimeout` は即時実行に差し替え）。

| # | fetchの挙動 | 期待結果 |
|---|------------|---------|
| 01 | 1回目成功（200） | Responseを返す、fetchは1回呼ばれる |
| 02 | 1回目失敗→2回目成功 | Responseを返す、fetchは2回呼ばれる |
| 03 | 1回目〜3回目失敗→4回目成功 | Responseを返す、fetchは4回呼ばれる |
| 04 | 4回連続失敗 | エラーをthrow、fetchは4回呼ばれる（初回+3リトライ） |
| 05 | 1回目 `response.ok=false`（status:400） | `"HTTP error: 400"` のエラーをthrow |
| 06 | 1回目・2回目失敗→3回目成功 | sleepの待機時間が `[1000, 2000]`（指数バックオフ） |
| 07 | 1回目失敗→2回目成功 | `console.error` に `[Calil] ERROR` プレフィックスが含まれる |

---

### UT-API-03: `checkLibrary` — 蔵書検索（ポーリング込み）

`fetchWithRetry` と `parseResponse` をモック（または `fetch` をモック）する。

| # | シナリオ | 期待結果 |
|---|---------|---------|
| 01 | 初回レスポンスで `continue=0` | ポーリングなしで `books[isbn][systemid]` を返す（fetchは1回） |
| 02 | 初回 `continue=1`、2回目 `continue=0` | 1回ポーリング後に結果を返す（fetchは2回） |
| 03 | 10回 `continue=1` 後に `continue=0` | 10回ポーリングして結果を返す（fetchは11回） |
| 04 | 11回連続 `continue=1` | `Error('polling_timeout')` をthrow（fetchは11回） |
| 05 | `books[isbn][systemid]` が存在しない（booksが空オブジェクト） | `null` を返す |
| 06 | ポーリング時のURLに `session` パラメータが含まれる | fetch第2引数URLに `session=xxxx` が含まれる |
| 07 | 正常完了時のログ | `console.log` に `[Calil] REQUEST` が含まれる |
| 08 | 正常完了時のログ | `console.log` に `[Calil] RESPONSE` が含まれる |
| 09 | ポーリングが発生した場合のログ | `console.log` に `[Calil] POLLING` と `[Calil] POLLING RESPONSE` が含まれる |

---

### UT-API-04: `checkLibraries` — 複数図書館の並列取得

`checkLibrary` をモックする。

| # | シナリオ | 期待結果 |
|---|---------|---------|
| 01 | 2図書館すべて成功 | `[{library, result, error:null}, {library, result, error:null}]` |
| 02 | 1館成功・1館失敗（fetch URLで分岐） | 失敗館は `{library, result:null, error:truthy}` になる |
| 03 | 2図書館すべて失敗 | 両方 `error` が設定される（`Promise.allSettled` なのでthrowしない） |
| 04 | `libraries` が空配列 | 空配列 `[]` を返し、fetchは呼ばれない |
| 05 | 3図書館（Alpha/Beta/Gamma）すべて成功 | `results[i].library.systemid` が入力の順序と一致する |

---

### UT-API-05: `searchLibraries` — 図書館検索

`fetch` をモックする。

| # | シナリオ | 期待結果 |
|---|---------|---------|
| 01 | 正常レスポンス（配列JSON） | 図書館オブジェクトの配列を返す |
| 02 | 正常レスポンス（JSONP配列） | JSONP形式でも配列を返す |
| 03 | レスポンスが配列でない（オブジェクト） | 空配列 `[]` を返す |
| 04 | `city` 未指定 | URLに `city` パラメータが含まれない |
| 05 | `city` 指定あり | URLに `city=渋谷区`（URLデコード後）が含まれる |
| 06 | 正常完了時のログ | `console.log` に `[Calil] REQUEST` と `[Calil] RESPONSE` が含まれる |

---

## 6. サービスワーカー メッセージハンドラ（`background/service_worker.js`）

`chrome.storage.sync`・`checkLibraries`・`searchLibraries` をモックする。

### UT-SW-01: `GET_SETTINGS` — ストレージから設定取得

メッセージ `{type: "GET_SETTINGS"}` を送信して動作確認する。

| # | `chrome.storage.sync.get` の挙動 | 期待結果 |
|---|--------------------------------|---------|
| 01 | `{calil_api_key:"my_api_key", libraries:[...]}` を返す | `{success:true, data:{calil_api_key:"my_api_key", libraries:[...]}}` |
| 02 | `calil_api_key` が含まれない | `data.calil_api_key` が空文字 `""` になる |
| 03 | `libraries` が含まれない | `data.libraries` が空配列 `[]` になる |
| 04 | `lastError` が設定されている | `{success:false}` を返す |

---

### UT-SW-02: `CHECK_LIBRARY` — 蔵書確認ハンドラ

メッセージ `{type: "CHECK_LIBRARY", payload: {...}}` を送信して動作確認する。

| # | 前提 | 期待結果 |
|---|------|---------|
| 01 | APIキー・図書館リスト設定済み | `checkLibraries(apiKey, isbn, libraries)` が呼ばれ、`{success:true, data:[...]}` を返す |
| 02 | APIキー未設定（空文字） | `{success:false, error:"api_key_not_set"}` を返す |
| 03 | 図書館リストが空配列かつpayloadにも指定なし | `{success:false, error:"no_libraries_set"}` を返す |
| 04 | `payload.libraries` に別の図書館リストを指定 | ストレージのlibrariesではなくpayloadの値で `checkLibraries` が呼ばれる |
| 05 | `checkLibraries` がエラーをthrow | `{success:false, error:"<エラーメッセージ>"}` を返す |

---

### UT-SW-03: `SEARCH_LIBRARIES` — 図書館検索ハンドラ

メッセージ `{type: "SEARCH_LIBRARIES", payload: {...}}` を送信して動作確認する。

| # | 前提 | 期待結果 |
|---|------|---------|
| 01 | `payload.appkey` あり | `searchLibraries(payloadKey, pref, city)` が呼ばれる（ストレージキーは無視） |
| 02 | `payload.appkey` なし・ストレージにAPIキーあり | `searchLibraries(storageKey, pref, city)` が呼ばれる |
| 03 | `payload.appkey` なし・ストレージのAPIキーも空 | `{success:false, error:"api_key_not_set"}` を返す |
| 04 | `searchLibraries` が正常に配列を返す | `{success:true, data:[...]}` を返す |

---

### UT-SW-04: `OPEN_OPTIONS` — オプションページを開く

メッセージ `{type: "OPEN_OPTIONS"}` を送信して動作確認する。

| # | 確認内容 | 期待結果 |
|---|---------|---------|
| 01 | `chrome.runtime.openOptionsPage` の呼び出し | 1回呼ばれる |
| 02 | メッセージハンドラの戻り値 | `false`（同期処理のためasyncではない） |

---

## 7. ポップアップ — 集約表示ロジック（`popup/popup.js`）

### UT-POP-01: `getStatusInfo` — libkeyからシステム単位の代表ステータス取得

```
関数シグネチャ: getStatusInfo(libkey: Object) → {dotClass: string, label: string}
```

| # | 入力 `libkey` | 期待 `dotClass` | 期待 `label` | 説明 |
|---|--------------|---------------|-------------|------|
| 01 | `{"中央": "貸出可", "多摩": "貸出中"}` | `"available"` | `"貸出可"` | 貸出可が貸出中より優先 |
| 02 | `{"中央": "貸出中", "多摩": "蔵書なし"}` | `"on-loan"` | `"貸出中"` | 貸出中が蔵書なしより優先 |
| 03 | `{"中央": "蔵書なし"}` | `"none"` | `"蔵書なし"` | 蔵書なし（単一館） |
| 04 | `{}` | `"none"` | `"蔵書なし"` | 空オブジェクト → デフォルト |
| 05 | `null` | `"none"` | `"蔵書なし"` | null → デフォルト |
| 06 | `{"中央": "館内のみ"}` | `"other"` | `"館内のみ"` | その他状態 |
| 07 | `{"中央": "予約中"}` | `"other"` | `"予約中"` | その他状態 |
| 08 | `{"中央": "蔵書なし", "北": "蔵書なし", "南": "蔵書なし"}` | `"none"` | `"蔵書なし"` | 全館蔵書なし |
| 09 | `{"北": "館内のみ", "南": "貸出可"}` | `"available"` | `"貸出可"` | 貸出可が館内のみより優先 |
| 10 | `{"北": "館内のみ", "南": "貸出中"}` | `"on-loan"` | `"貸出中"` | 貸出中が館内のみより優先 |
| 11 | `{"中央": "貸出可"}` | `"available"` | `"貸出可"` | 単一館・貸出可 |
| 12 | `{"中央": "貸出中"}` | `"on-loan"` | `"貸出中"` | 単一館・貸出中 |

---

## 8. ページ判定ロジック（`content_script.js`）

> ⚠️ **このセクション（UT-PAGE-01〜03）は現時点でテストコードが実装されていません。**
> 仕様としては定義済みですが、対応するテストファイルが存在しないため、テスト実行時には検証されません。

jsdom でDOMと `window.location` を設定した上でテストする。

### UT-PAGE-01: `isRakutenBookPage` — 楽天ブックス書籍ページ判定 ※未実装

```
関数シグネチャ: isRakutenBookPage() → boolean
```

| # | `location.href` | 期待値 |
|---|-----------------|--------|
| 01 | `"https://books.rakuten.co.jp/rb/17819239/"` | `true` |
| 02 | `"https://books.rakuten.co.jp/rb/1234567890"` | `true` |
| 03 | `"https://books.rakuten.co.jp/"` | `false` |
| 04 | `"https://books.rakuten.co.jp/search/"` | `false` |
| 05 | `"https://www.rakuten.co.jp/rb/12345/"` | `false` | ドメインが異なる |
| 06 | `"https://books.rakuten.co.jp/rb/abc/"` | `false` | 数字以外のID |

---

### UT-PAGE-02: `isAmazonBookPage` — Amazonの書籍ページ判定 ※未実装

jsdomでDOM要素と `location.href` を設定する。

| # | `location.href` | DOM | 期待値 |
|---|-----------------|-----|--------|
| 01 | `.../dp/4873117380` | `#detailBullets_feature_div` あり + 「ISBN-10」テキスト | `true` |
| 02 | `.../dp/4873117380` | `#rpi-attribute-book_details-isbn13` あり + 「ISBN-13」テキスト | `true` |
| 03 | `.../dp/B09JQMJHXY` | DOM要素なし | `false` | 家電商品 |
| 04 | `.../dp/4873117380` | DOM要素あり + ISBNテキストなし | `false` |
| 05 | `https://www.amazon.co.jp/s?k=python` | DOM要素あり | `false` | `/dp/` なし |
| 06 | `.../dp/4873117380` | DOM要素なし + ISBNテキストあり | `false` | 両条件AND |

---

### UT-PAGE-03: `extractIsbnFromRakuten` — 楽天ブックスページからのISBN抽出 ※未実装

jsdomでDOMを構築する。抽出の優先順位を確認する。

#### 優先順位1: `<meta property="books:isbn">`

| # | DOM | 期待値 |
|---|-----|--------|
| 01 | `<meta property="books:isbn" content="9784873117386">` | `"9784873117386"` |
| 02 | `<meta name="isbn" content="9784873117386">` | `"9784873117386"` |

#### 優先順位2: th/dt要素の走査

| # | DOM | 期待値 |
|---|-----|--------|
| 03 | `<th>ISBN</th><td>9784873117386</td>` | `"9784873117386"` |
| 04 | `<dt>ISBN</dt><dd>4873117380</dd>` | `"9784873117386"` （ISBN-10→13変換） |
| 05 | `<th>発売日</th><td>2024年</td>` のみ | `null`（ISBNラベルなし） |

#### 優先順位3: ページ本文テキスト検索

| # | `document.body.innerText` | 期待値 |
|---|--------------------------|--------|
| 06 | `"ISBN：9784873117386"` | `"9784873117386"` |
| 07 | `"ISBN: 978-4-87311-738-6"` | `"9784873117386"` |

#### 優先順位4: URLのパスから抽出

| # | `location.pathname` | 期待値 |
|---|---------------------|--------|
| 08 | `"/rb/9784873117386/"` | `"9784873117386"` |
| 09 | `"/rb/4873117380/"` | `"9784873117386"` （10桁→変換） |

---

## 9. テストコード例（参考実装）

### isbn.test.js（純粋関数のテスト例）

```js
// jest設定: testEnvironment: 'node'
const { calcIsbn13CheckDigit, isbn10to13, toIsbn13 } = require('../content/isbn-utils');
// または content_script.js からエクスポートされた関数を使用

describe('calcIsbn13CheckDigit', () => {
  test('978487311738 → 6', () => {
    expect(calcIsbn13CheckDigit('978487311738')).toBe(6);
  });

  test('チェックデジットが0になるケース', () => {
    expect(calcIsbn13CheckDigit('978000000000')).toBe(0);
  });
});

describe('toIsbn13', () => {
  test('ISBN-13はそのまま返す', () => {
    expect(toIsbn13('9784873117386')).toBe('9784873117386');
  });

  test('ハイフン付きISBN-13を正規化', () => {
    expect(toIsbn13('978-4-87311-738-6')).toBe('9784873117386');
  });

  test('ISBN-10をISBN-13に変換', () => {
    expect(toIsbn13('4873117380')).toBe('9784873117386');
  });

  test('不正な文字列はnullを返す', () => {
    expect(toIsbn13('invalid')).toBeNull();
    expect(toIsbn13('')).toBeNull();
    expect(toIsbn13('123456789012')).toBeNull();
  });
});
```

### calil.test.js（fetch モックのテスト例）

```js
// jest設定: testEnvironment: 'node'
const { parseResponse, checkLibraries } = require('../api/calil');

describe('parseResponse', () => {
  const mockResponse = (text) => ({ text: () => Promise.resolve(text) });

  test('正規JSONをパースする', async () => {
    const result = await parseResponse(mockResponse('{"continue":0}'));
    expect(result).toEqual({ continue: 0 });
  });

  test('JSONP形式をパースする', async () => {
    const result = await parseResponse(mockResponse('callback({"continue":0})'));
    expect(result).toEqual({ continue: 0 });
  });

  test('不正なフォーマットはエラーをthrowする', async () => {
    await expect(parseResponse(mockResponse('invalid data'))).rejects.toThrow();
  });
});

describe('checkLibraries', () => {
  test('一部失敗してもPromise.allSettledで全結果を返す', async () => {
    // checkLibrary をモックして1館成功・1館失敗を再現
    const libs = [
      { systemid: 'Tokyo_Pref', name: '東京' },
      { systemid: 'Osaka_Pref', name: '大阪' },
    ];
    // ...モック設定...
    const results = await checkLibraries('key', '9784873117386', libs);
    expect(results).toHaveLength(2);
    expect(results[0].error).toBeNull();
    expect(results[1].error).not.toBeNull();
  });
});
```

---

## 10. カバレッジ目標

| モジュール | 目標行カバレッジ | 優先度 |
|-----------|--------------|--------|
| ISBN変換ユーティリティ（純粋関数） | 100% | 高 |
| `sanitizeText` / `sanitizeUrl` | 100% | 高 |
| `getBranchIconClass` | 100% | 高 |
| `getCacheKey` | 100% | 高 |
| `parseResponse`（calil.js） | 100% | 高 |
| `getStatusInfo`（popup.js） | 100% | 高 |
| `fetchWithRetry`（リトライロジック） | 90%以上 | 中 |
| `checkLibrary`（ポーリングロジック） | 80%以上 | 中 |
| `checkLibraries`（並列処理） | 80%以上 | 中 |
| `handleCheckLibrary` / `handleSearchLibraries` | 80%以上 | 中 |
| `setWidgetResults`（DOM生成） | 70%以上 | 低 |
| `isAmazonBookPage` / `isRakutenBookPage` | 70%以上 | 低 |
| `extractIsbnFromPage` / `extractIsbnFromRakuten` | 70%以上 | 低 |
