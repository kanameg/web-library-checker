# Chrome拡張機能「図書館蔵書チェッカー」仕様書

## 1. 概要

Amazon.co.jp および 楽天ブックス の書籍ページを閲覧中に、ユーザーが設定した図書館にその書籍の蔵書があるかどうかをリアルタイムで確認し、ページ上に表示するChrome拡張機能。

---

## 2. 機能要件

### 2.1 基本機能

| 機能 | 説明 |
|------|------|
| 書籍ページ検出 | Amazon.co.jp・楽天ブックスの書籍商品ページを自動検出する |
| ISBN取得 | ページのDOM・URLから書籍のISBNを抽出する |
| 蔵書確認 | カーリルAPIを使用して、設定済み図書館の蔵書・貸出状況を検索する |
| 結果表示 | 蔵書状況を各サイトのページ内にインライン表示する |
| 図書館設定 | ユーザーが利用したい図書館を設定・保存する |

### 2.2 表示する情報

- 蔵書の有無
- 貸出状況（貸出可 / 貸出中 / 館内のみ / 予約中 / 準備中 / 休館中 / 蔵書なし）
- 図書館の予約ページへのリンク（`reserveurl`）

---

## 3. 技術仕様

### 3.1 使用API

**カーリル図書館API**（[公式ドキュメント](https://calil.jp/doc/api_ref.html)）

#### 蔵書検索エンドポイント

```
GET https://api.calil.jp/check
```

| パラメータ | 必須 | 説明 |
|-----------|------|------|
| `appkey`  | ○ | カーリルAPIのアプリケーションキー |
| `isbn`    | ○ | 書籍のISBN（10桁または13桁） |
| `systemid`| ○ | 図書館システムID（例: `Tokyo_Pref`） |
| `format`  | - | `json`を指定 |
| `callback`| - | JSONPを使用する場合のコールバック関数名 |

#### 図書館検索エンドポイント（設定画面で使用）

```
GET https://api.calil.jp/library
```

| パラメータ | 必須 | 説明 |
|-----------|------|------|
| `appkey`  | ○ | アプリケーションキー |
| `pref`    | △ | 都道府県名（例: `東京都`）※いずれか1つ必須 |
| `city`    | △ | 市区町村名（`pref`とセットで使用） |
| `geocode` | △ | 緯度経度（例: `139.6917,35.6895`） |
| `format`  | - | `json`を指定 |

#### ポーリング仕様

カーリルAPIの蔵書検索は非同期処理のため、レスポンスの `continue` フィールドが `1` の場合は `session` パラメータを付与して **2秒以上の間隔** で再リクエストが必要。

```
レスポンス例:
{
  "session": "xxxxxxxx",
  "books": {
    "9784000000000": {
      "Tokyo_Pref": {
        "status": "OK",
        "reserveurl": "https://...",
        "libkey": {
          "東京都立図書館": "貸出可"
        }
      }
    }
  },
  "continue": 0
}
```

### 3.2 ファイル構成

```
extension/
├── manifest.json          # 拡張機能マニフェスト（Manifest V3）
├── background/
│   └── service_worker.js  # バックグラウンドサービスワーカー
├── content/
│   └── content_script.js  # Amazon・楽天ブックスページへの注入スクリプト
├── popup/
│   ├── popup.html         # ポップアップUI
│   ├── popup.css          # ポップアップスタイル
│   └── popup.js           # ポップアップロジック
├── options/
│   ├── options.html       # 設定ページUI
│   ├── options.css        # 設定ページスタイル
│   └── options.js         # 設定ページロジック
├── api/
│   └── calil.js           # カーリルAPI呼び出しモジュール
├── utils/
│   ├── isbn.js            # ISBN抽出・変換ユーティリティ
│   ├── sanitize.js        # HTML/URLサニタイズユーティリティ
│   ├── status.js          # 貸出ステータス→CSSクラスマッピング
│   └── storage.js         # Chrome Storage読み込みユーティリティ
└── icons/
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

### 3.3 マニフェスト（manifest.json）

```json
{
  "manifest_version": 3,
  "name": "図書館蔵書チェッカー",
  "version": "1.0.0",
  "description": "Amazon・楽天ブックスの書籍ページで図書館の蔵書状況を確認できます",
  "permissions": [
    "storage",
    "activeTab"
  ],
  "host_permissions": [
    "https://www.amazon.co.jp/*",
    "https://books.rakuten.co.jp/*",
    "https://api.calil.jp/*"
  ],
  "background": {
    "service_worker": "background/service_worker.js"
  },
  "content_scripts": [
    {
      "matches": [
        "https://www.amazon.co.jp/*",
        "https://books.rakuten.co.jp/*"
      ],
      "js": ["utils/isbn.js", "utils/sanitize.js", "utils/status.js", "content/content_script.js"],
      "css": ["content/content_script.css"],
      "run_at": "document_idle"
    }
  ],
  "action": {
    "default_popup": "popup/popup.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },
  "options_ui": {
    "page": "options/options.html",
    "open_in_tab": true
  }
}
```

---

## 4. 書籍ページ判定とISBN抽出

### 4.1 書籍ページの判定条件

#### Amazon.co.jp

以下の条件をANDで評価する：

1. URLが `https://www.amazon.co.jp/` で始まり、`/dp/[ASIN]` パターンを含む
2. 商品ページのDOMに `#detail-bullets`、`#productDetailsTable`、`#detailBullets_feature_div`、`#rpi-attribute-book_details-isbn13`、`#rpi-attribute-book_details-isbn10` のいずれかが存在する
3. ページ内に「ISBN-10」または「ISBN-13」のテキストが存在する

#### 楽天ブックス

以下の条件をANDで評価する：

1. URLが `https://books.rakuten.co.jp/rb/数字` の形式（書籍詳細ページ）

> URLパターンのみで判定する（DOM構造への依存を排除）。

### 4.2 ISBNの抽出方法

#### Amazon.co.jp

優先順位順に試みる：

1. **URLのASINから変換**
   - URL例: `https://www.amazon.co.jp/dp/4000000000`
   - ASIN（10桁の数字）= ISBN-10 → ISBN-13 に変換
   - 変換式: 先頭に `978` を付加してチェックデジットを再計算

2. **商品詳細セクションのDOM解析**
   - セレクタ（ISBN-13 新形式）: `#rpi-attribute-book_details-isbn13 .rpi-attribute-value`
   - セレクタ（ISBN-10 新形式）: `#rpi-attribute-book_details-isbn10 .rpi-attribute-value`（ISBN-13に変換して使用）
   - セレクタ（旧形式）: `#detail-bullets .content li` および `#detailBullets_feature_div li`
     - 「ISBN-13:」「ISBN-10:」のラベルを持つ `li` 要素のテキストを抽出

3. **メタタグ**
   - `<meta name="books:isbn" content="...">` を参照

#### 楽天ブックス

優先順位順に試みる：

1. **メタタグ**
   - `<meta property="books:isbn" content="...">` を参照
   - `<meta name="isbn" content="...">` を参照

2. **ページ内のth/dt要素のDOM解析**
   - ページ内の全 `th`・`dt` 要素を走査し、「ISBN」テキストを含む要素の次の兄弟要素（`td`・`dd`）の値を抽出
   - 特定のクラス名に依存しない汎用的な解析を行う

3. **ページ本文テキストからのパターン検索**
   - `document.body.innerText` からISBN-13パターン（`97[89][-\d]{10,17}`）を検索
   - 続いてISBN-10パターン（`[0-9]{9}[0-9X]`）を検索

4. **URLのパスから抽出**
   - URL例: `https://books.rakuten.co.jp/rb/12345678/`
   - パス末尾の数値ID（9〜13桁）をISBNとして試みる（10桁の場合はISBN-13に変換）

### 4.3 ISBN変換ロジック（isbn.js）

`utils/isbn.js` が提供する関数一覧:

| 関数 | 引数 | 戻り値 | 説明 |
|------|------|--------|------|
| `calcIsbn13CheckDigit(digits12)` | `string` 先頭12桁 | `number` | ISBN-13 チェックデジット計算 |
| `isbn10to13(isbn10)` | `string` ISBN-10 | `string\|null` | ISBN-10 → ISBN-13 変換 |
| `normalizeIsbn(isbn)` | `string` | `string` | ハイフン・スペースを除去して正規化 |
| `isValidIsbn13(isbn)` | `string` | `boolean` | ISBN-13 としてチェックデジット込みで有効か検証 |
| `isValidIsbn10(isbn)` | `string` | `boolean` | ISBN-10 としてチェックデジット込みで有効か検証 |
| `toIsbn13(isbn)` | `string` ISBN-10 or 13 | `string\|null` | 任意のISBN文字列をISBN-13に変換（チェックデジット検証付き、無効でも形式が合えば変換を試みる） |
| `extractAsinFromUrl(url)` | `string` URL | `string\|null` | Amazon URLから `/dp/[A-Z0-9]{10}` パターンでASINを抽出 |
| `extractIsbnFromPage()` | なし | `string\|null` | AmazonページのDOMからISBN-13を抽出（4.2の優先順位で検索） |
| `extractIsbnFromRakuten()` | なし | `string\|null` | 楽天ブックスページのDOMからISBN-13を抽出（4.2の優先順位で検索） |

> `utils/isbn.js` はコンテンツスクリプトから利用するため、`manifest.json` の `content_scripts.js` 配列に先頭から読み込まれる。

### 4.4 その他ユーティリティモジュール

#### sanitize.js

| 関数 | 引数 | 戻り値 | 説明 |
|------|------|--------|------|
| `sanitizeText(text)` | `any` | `string` | `document.createTextNode` 経由でHTMLエスケープした文字列を返す（XSS対策） |
| `sanitizeUrl(url)` | `string` | `string` | `http:` / `https:` 以外のプロトコルは `'#'` を返す。無効なURLも `'#'` を返す |

#### status.js

| 関数 | 引数 | 戻り値 | 説明 |
|------|------|--------|------|
| `getStatusClass(status)` | `string` カーリルAPIのステータス文字列 | `string` CSSクラス名 | ステータスを対応するCSSクラスに変換する |

ステータスとCSSクラスのマッピング:

| ステータス | CSSクラス | 表示色 |
|-----------|----------|-------|
| `貸出可` | `available` | 緑（`#2e7d32`） |
| `貸出中` | `on-loan` | 黄（`#f57f17`） |
| `休館中` | `none` | グレー（`#9e9e9e`） |
| `蔵書なし` | `none` | グレー（`#9e9e9e`） |
| 上記以外（`館内のみ`・`予約中`・`準備中` など） | `other` | 青（`#1565c0`） |

#### storage.js

| 関数 | 引数 | 戻り値 | 説明 |
|------|------|--------|------|
| `getSettings()` | なし | `Promise<{calil_api_key: string, libraries: Array}>` | `chrome.storage.sync` から APIキーと図書館リストを取得する |

> 設定の**保存**は各モジュール（`options.js`・`service_worker.js`）が直接 `chrome.storage.sync.set()` を呼び出す。`storage.js` は読み込み専用のヘルパーとして提供する。

---

## 5. UI仕様

### 5.1 ページ内インジェクション

書籍ページと判定された場合、各サイトのページ内に蔵書確認ウィジェットを挿入する。

#### Amazon.co.jp

**挿入位置:** `#richProductInformation_feature_div`（詳細情報：ISBN・出版社・ページ数・サイズ）の直後
**代替挿入位置:** `#detailBullets_feature_div`、`#bookDescription_feature_div`、`#centerCol` / `#ppd` / `#dp` 末尾、`body` の順で探索しappend

> `#buybox` / `#desktop_buybox` は右ペインに属するため対象外とする。
>
> `#centerCol` 内の要素順序（実ページ解析結果）:
> 1. `#titleblock_feature_div` — タイトル・著者
> 2. `#bookDescription_feature_div` — 内容紹介
> 3. `#richProductInformation_feature_div` — **詳細情報（ISBN・出版社・ページ数・サイズ）** ← ここの直後
> 4. レコメンド・バンドルなど
> 5. `#detailBullets_feature_div` — 旧形式の詳細情報
> 6. `#editorialReviews_feature_div` — 編集レビュー
> 7. `#customer-reviews_feature_div` — カスタマーレビュー

#### 楽天ブックス

**挿入位置:** `#itemCouponArea`（クーポンエリア）の直前
**代替挿入位置:** `#productDetailedDescription`（商品情報）の直前、`#productDetailedDescription` の直後、`#main` 末尾、`body` の順で探索

> 実際のページDOM構造（解析結果）:
>
> ```
> #main
>   .mainInner
>     .productWrapper.item
>       #productExtra         — 左カラム（書影スライダー）
>       #productInfo          — 右カラム（タイトル・価格・ポイント・クーポン）
>         #productTitle         — タイトル・著者
>         .productInfoArea      — レビュー・価格・ポイント表示
>         #react-other-format   — 他フォーマットリンク
>         #itemCouponArea       — ★クーポンエリア（ここの直前に挿入）
>         .benefitSection       — クーポン・キャンペーン一覧
>         #pitariFlgNewUser-pc  — レコメンド系
>     #productDetailedDescription — 商品情報（発売日・出版社・ISBN）
>     #editArea2 / #intro         — 商品説明
>     #monopolyAll                — 関連商品カルーセル
>     #itemReview / #productReview / #rankingDetail
>   #extra                    — 右ペイン（購入ボックス）
>     #purchaseBox
> ```
>
> `#itemCouponArea` の直前に挿入することで、価格・ポイント確認の直後でクーポン一覧の上という視認しやすい位置に表示できる。
> 購入ボックス（`#purchaseBox`）は右ペイン（`#extra`）に属するため対象外とする。

#### ウィジェットの状態と表示

| 状態 | 表示内容 |
|------|---------|
| 読み込み中 | スピナー + 「図書館蔵書を確認中...」 |
| 設定未完了 | 「図書館を設定してください」+ 設定画面へのリンク |
| 書籍ページ非対象 | 非表示 |
| APIエラー | 赤色アイコン + 「確認に失敗しました」 |

#### 図書館システム単位の表示

1図書館システム（`systemid`）につき1ブロックを生成する。

| 状態 | 表示内容 |
|------|---------|
| システム行（ヘッダ） | 図書館システム名（`systemname`、なければ `name`）+ 予約ページリンク（`reserveurl` がある場合） |
| 蔵書なし（`libkey` が空） | グレーアイコン + 「この図書館には蔵書がありません」 |
| APIエラー（システム単位） | 赤色アイコン + 「確認に失敗しました」 |

#### 各館（ブランチ）単位の表示

カーリルAPIレスポンスの `libkey` オブジェクト（館名 → 貸出状態）を展開し、**館ごとに1行**を表示する。

| 貸出状態 | アイコン色 | ラベル文字色 | 表示ラベル |
|---------|-----------|------------|-----------|
| `貸出可` | 緑色（`#2e7d32`） | 緑色（`#2e7d32`） | 貸出可 |
| `貸出中` | 黄色（`#f57f17`） | 黄色（`#f57f17`） | 貸出中 |
| `館内のみ` | 青色（`#1565c0`） | 青色（`#1565c0`） | 館内のみ |
| `予約中` | 青色（`#1565c0`） | 青色（`#1565c0`） | 予約中 |
| `準備中` | 青色（`#1565c0`） | 青色（`#1565c0`） | 準備中 |
| `休館中` | グレー（`#9e9e9e`） | グレー（`#9e9e9e`） | 休館中 |
| 上記以外 | 青色（`#1565c0`） | 青色（`#1565c0`） | そのまま表示 |

各館行の構成: `[状態アイコン] [館名（libkeyのキー）] [貸出状態（アイコンと同色）]`

> `libkey` の優先順位（ステータスアイコン決定用）：貸出可 > 貸出中 > 館内のみ・予約中・準備中 > 休館中 > 蔵書なし

#### ウィジェットHTMLイメージ

```html
<div id="calil-library-checker">
  <div class="calil-header">
    <span class="calil-header-icon">📚</span> 図書館蔵書チェッカー
  </div>
  <div class="calil-body">
    <!-- 図書館システムごとにブロックを生成 -->
    <div class="calil-system-block">
      <div class="calil-system-header">
        <span class="calil-system-name">東京都立図書館</span>
        <a href="https://..." class="calil-reserve-link" target="_blank" rel="noopener noreferrer">予約する</a>
      </div>
      <!-- libkey の館ごとに1行 -->
      <div class="calil-branch-row">
        <span class="calil-status-icon available"></span>
        <span class="calil-branch-name">中央図書館</span>
        <span class="calil-branch-status">貸出可</span>
      </div>
      <div class="calil-branch-row">
        <span class="calil-status-icon on-loan"></span>
        <span class="calil-branch-name">多摩図書館</span>
        <span class="calil-branch-status">貸出中</span>
      </div>
    </div>
    <!-- 蔵書なしの場合 -->
    <div class="calil-system-block">
      <div class="calil-system-header">
        <span class="calil-system-name">千代田区立図書館</span>
      </div>
      <div class="calil-branch-row">
        <span class="calil-status-icon none"></span>
        <span class="calil-branch-status calil-no-stock">この図書館には蔵書がありません</span>
      </div>
    </div>
  </div>
  <div class="calil-footer">
    <a href="#" id="calil-settings-link">設定を変更</a>
    <a href="https://calil.jp/" target="_blank" rel="noopener noreferrer">Powered by カーリル</a>
  </div>
</div>
```

### 5.2 ポップアップ（popup.html）

現在ページの蔵書状況をコンパクトに表示。

- 図書館名（`systemname`、なければ `name`）と現在の設定を表示
- 蔵書状況を図書館システム単位で一覧表示（各システムの代表ステータスを表示）
- 「設定」ボタンで options.html を開く

#### ポップアップの動作フロー

1. アクティブタブのURLを確認し、対象サイト（`amazon.co.jp` / `books.rakuten.co.jp`）かどうかを判定
2. 対象外サイトの場合は「このページは対応していません」を表示して終了
3. コンテンツスクリプトに `GET_PAGE_INFO` メッセージを送信し、書籍ページ判定結果とISBNを取得（例外発生時はコンテンツスクリプト未注入とみなし非対応表示で終了）
4. 書籍ページでない（`isBookPage === false`）・ISBNが取得できない場合は非対応表示
5. サービスワーカーに `GET_SETTINGS` メッセージを送信して設定を取得
6. APIキー・図書館未設定の場合は設定画面への誘導を表示
7. サービスワーカーに `CHECK_LIBRARY` メッセージを送信してAPI結果を取得
8. 各図書館システムについて代表ステータス（貸出可 > 貸出中 > その他 > 蔵書なし の優先順位）を表示

#### 蔵書ステータスの集約表示（図書館システム単位）

| 優先順位 | 条件 | 表示 | ドット色クラス |
|---------|------|------|--------------|
| 1 | `libkey` に `貸出可` が含まれる | 貸出可 | `available` |
| 2 | `libkey` に `貸出中` が含まれる | 貸出中 | `on-loan` |
| 3 | `libkey` が空または `蔵書なし` | 蔵書なし | `none` |
| 4 | 上記以外 | `libkey` の先頭値 | `other` |

#### ヘッダースタイル

| 要素 | 値 |
|------|---|
| 背景色（`.popup-header`） | 白（`#ffffff`） |
| 文字色（`.popup-header`） | 黒（`#333333`） |
| 下ボーダー | `1px solid #eeeeee` |

### 5.3 設定ページ（options.html）

#### 設定項目

| 設定項目 | 説明 |
|---------|------|
| APIキー | カーリルAPIのアプリケーションキー（必須）|
| 図書館の検索 | 都道府県・市区町村で検索して図書館を選択 |
| 選択中の図書館 | 現在設定している図書館の一覧（最大5件）|

#### 図書館選択フロー

1. 都道府県をセレクトボックスで選択
2. 市区町村名を入力（任意）
3. 「検索」ボタンで `GET /library` を呼び出す
4. 結果の図書館リストを表示（表示名は `formal` を優先し、なければ `systemname` を使用）
5. チェックボックスで最大5館を選択（上限に達すると未選択のチェックボックスは無効化）
6. 「保存」で `chrome.storage.sync` に保存

---

## 6. データフロー

```
[Amazon書籍ページ または 楽天ブックス書籍ページ 読み込み]
        ↓
[content_script.js起動]
        ↓
[サイト判定（Amazon / 楽天ブックス）]
        ↓
[書籍ページ判定] → 非書籍 → 処理終了
        ↓ 書籍
[ISBNをDOMから抽出（サイト別ロジック）]
        ↓
[chrome.storage.syncから設定を読み込み]
        ↓
[設定未完了] → ウィジェットに案内を表示 → 処理終了
        ↓ 設定済み
[ウィジェットを挿入（ローディング状態）]
        ↓
[calil.js: 各図書館システムに対して並列（Promise.allSettled）でAPIリクエスト]
  [GET /check?isbn=...&systemid=...&format=json]
          ↓
  [continue=1の場合] → 2秒待機 → sessionパラメータ付きで再リクエスト（最大10回）
          ↓ continue=0
  [レスポンス解析]
        ↓ 全図書館の結果が揃う（エラー館はerrorフラグ付きで返す）
[ウィジェットの表示を蔵書状況に更新]
```

---

## 7. ストレージ設計

`chrome.storage.sync` に以下の形式で保存する。

```json
{
  "calil_api_key": "your_appkey_here",
  "libraries": [
    {
      "systemid": "Tokyo_Pref",
      "name": "東京都立図書館",
      "systemname": "東京都立",
      "pref": "東京都",
      "city": ""
    }
  ]
}
```

---

## 8. エラーハンドリング

| エラー | 対処 |
|--------|------|
| ISBNが取得できない | ウィジェットを非表示（書籍として扱わない） |
| APIキー未設定 | 設定画面への誘導メッセージを表示 |
| ネットワークエラー | リトライ（最大3回、exponential backoff）後にエラー表示 |
| ポーリングタイムアウト（10回超過） | タイムアウトエラーとして表示 |
| APIエラーレスポンス | エラー内容をコンソールに記録し、ユーザーにはエラー表示 |

---

## 9. 利用制限への対応

カーリルAPIの利用制限（1000リクエスト/時）に対し、以下のキャッシュ戦略を実装する。

- **セッションキャッシュ:** `sessionStorage` に `calil_{isbn}_{systemids}` をキーとして蔵書状況をキャッシュ（同一タブ内での重複リクエスト防止）
  - `systemids` は設定済み図書館の `systemid` をアルファベット順ソートしてカンマ区切りで結合した文字列
  - 例: `calil_9784873117386_Tokyo_Pref,Osaka_Pref`
- **ページ再訪問:** Amazonおよび楽天ブックスはページ遷移でDOMが再生成されるため、セッションキャッシュは次のページ訪問時にも有効な間は利用する

---

## 10. セキュリティ考慮事項

- APIキーは `chrome.storage.sync` に保存し、コンテンツスクリプトからは `chrome.runtime.sendMessage` 経由でサービスワーカーに処理を委譲する
- APIへのリクエストはサービスワーカーで実行し、APIキーがコンテンツスクリプト側に露出しないようにする
- 取得したURLや図書館名はDOM挿入前にサニタイズする（XSS対策）
  - テキストは `document.createTextNode` 経由で安全に挿入
  - URLは `new URL()` でパースし、`http:` / `https:` 以外のプロトコルは `#` に置換

### サービスワーカーが処理するメッセージ種別

| メッセージタイプ | 送信元 | 概要 |
|----------------|--------|------|
| `CHECK_LIBRARY` | コンテンツスクリプト・ポップアップ | 指定ISBNの蔵書状況をAPIで確認 |
| `SEARCH_LIBRARIES` | 設定ページ | 都道府県・市区町村で図書館を検索 |
| `GET_SETTINGS` | コンテンツスクリプト・ポップアップ | `chrome.storage.sync` から設定を取得 |
| `OPEN_OPTIONS` | コンテンツスクリプト・ポップアップ | 設定ページ（`options.html`）を開く |

コンテンツスクリプトは `GET_PAGE_INFO` メッセージをポップアップから受け取り、現在のページの書籍判定結果とISBNを返す（サービスワーカー経由ではなく直接通信）。

---

## 11. 開発・リリースフロー

1. [カーリルAPIキーの申請](https://calil.jp/doc/api.html) を行い、アプリケーションキーを取得する
2. 開発環境でのテスト（Chromeの「パッケージ化されていない拡張機能を読み込む」）
3. 機能テスト
   - Amazon書籍ページでのISBN検出テスト（新・旧DOM両対応）
   - 楽天ブックス書籍ページでのISBN検出テスト
   - ポーリングの動作確認
   - 設定画面での図書館検索・保存の確認
4. Chrome Web Store への公開申請

---

## 参考リンク

- [カーリル図書館API公式ドキュメント](https://calil.jp/doc/api.html)
- [カーリルAPIリファレンス](https://calil.jp/doc/api_ref.html)
- [Chrome拡張機能 Manifest V3 公式ドキュメント](https://developer.chrome.com/docs/extensions/mv3/intro/)
